<%# フォロー解除ボタン %>
<%= form_for(current_user, url: user_relationship_path(@user.id, @relationship.id), method: :delete, remote: true) do |f| %>
  <%= f.submit "フォロー解除", class: "btn-flat-border" %>
<% end %> 